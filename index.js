/**
 * @format
 */

import {AppRegistry} from 'react-native';
import DadJokes from './src/screens/DadJokes';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => DadJokes);
